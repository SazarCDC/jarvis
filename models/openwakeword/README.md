# openWakeWord ONNX models

Place ONNX files for offline wake-word detection in this folder:

- `melspectrogram.onnx`
- `embedding_model.onnx`
- `hey_jarvis_v0.1.onnx` (or another wake model)

Jarvis loads models from this directory by default.
